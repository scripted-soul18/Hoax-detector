<!DOCTYPE html>
<html>
<head>
  <title>Hoax detector</title>
  <style>
    /* CSS Variables for Theming */
    :root {
      --bg-gradient-start: #151817;
      --bg-gradient-end: #181010;
      --container-bg: rgb(253, 248, 218);
      --text-primary: #333;
      --text-secondary: #666;
      --navbar-bg: rgba(255, 255, 255, 0.95);
      --input-bg: white;
      --input-border: #e0e0e0;
      --result-bg: #f8f9fa;
      --modal-bg: white;
      --shadow-color: rgba(0, 0, 0, 0.1);
    }

    [data-theme="dark"] {
      --bg-gradient-start: #1a1a2e;
      --bg-gradient-end: #16213e;
      --container-bg: #2d3748;
      --text-primary: #e2e8f0;
      --text-secondary: #a0aec0;
      --navbar-bg: rgba(45, 55, 72, 0.95);
      --input-bg: #4a5568;
      --input-border: #718096;
      --result-bg: #4a5568;
      --modal-bg: #2d3748;
      --shadow-color: rgba(0, 0, 0, 0.5);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
      min-height: 100vh;
      padding: 20px;
      transition: background 0.5s ease;
      opacity: 0;
      animation: fadeInBody 0.8s ease forwards;
    }

    @keyframes fadeInBody {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    .navbar {
      background: var(--navbar-bg);
      padding: 15px 30px;
      border-radius: 10px;
      margin-bottom: 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 10px var(--shadow-color);
      opacity: 0;
      transform: translateY(-20px);
      animation: slideDown 0.6s ease 0.2s forwards;
      transition: background 0.5s ease, box-shadow 0.5s ease;
    }

    @keyframes slideDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .navbar h1, .logo {
      font-size: 1.5em;
      font-weight: 700;
      background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      transition: all 0.5s ease;
    }

    .nav-buttons {
      display: flex;
      gap: 10px;
    }

    .nav-btn {
      padding: 8px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
      transition: all 0.3s;
    }

    .nav-btn-primary {
      background: linear-gradient(135deg, #3dce96 0%, #af1926 100%);
      color: white;
    }

    .nav-btn-secondary {
      background: #6c757d;
      color: white;
    }

    .nav-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }

    .user-info {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .container {
      background: var(--container-bg);
      border-radius: 20px;
      box-shadow: 0 20px 60px var(--shadow-color);
      padding: 40px;
      max-width: 900px;
      width: 100%;
      margin: 0 auto;
      opacity: 0;
      transform: translateY(30px);
      animation: slideUp 0.8s ease 0.4s forwards;
      transition: background 0.5s ease, box-shadow 0.5s ease;
    }

    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    h2 {
      color: var(--text-primary);
      margin-bottom: 30px;
      text-align: center;
      font-size: 2em;
      background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      transition: color 0.5s ease;
      opacity: 0;
      animation: fadeInScale 0.6s ease 0.6s forwards;
    }

    @keyframes fadeInScale {
      from {
        opacity: 0;
        transform: scale(0.9);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    .input-tabs {
      display: flex;
      gap: 10px;
      margin-bottom: 15px;
      opacity: 0;
      animation: fadeIn 0.6s ease 0.8s forwards;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    .tab-btn {
      flex: 1;
      padding: 12px;
      border: 2px solid var(--input-border);
      background: var(--input-bg);
      border-radius: 10px;
      cursor: pointer;
      font-size: 16px;
      transition: all 0.3s ease;
      color: var(--text-primary);
    }

    .tab-btn.active {
      background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
      color: white;
      border-color: transparent;
    }

    .input-section {
      display: none;
      opacity: 0;
      transform: translateX(-20px);
    }

    .input-section.active {
      display: block;
    }

    .input-section.active.animate-in {
      animation: slideInFromLeft 0.6s ease forwards;
    }

    @keyframes slideInFromLeft {
      from {
        opacity: 0;
        transform: translateX(-20px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    textarea, input {
      width: 100%;
      padding: 15px;
      border: 2px solid var(--input-border);
      border-radius: 10px;
      font-size: 16px;
      font-family: inherit;
      resize: vertical;
      transition: border-color 0.3s ease, background 0.5s ease, color 0.5s ease;
      background: var(--input-bg);
      color: var(--text-primary);
    }

    textarea:focus, input:focus {
      outline: none;
      border-color: var(--bg-gradient-start);
    }

    textarea::placeholder, input::placeholder {
      color: var(--text-secondary);
    }

    input {
      resize: none;
    }

    button {
      background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
      color: white;
      border: none;
      padding: 15px 40px;
      font-size: 18px;
      border-radius: 10px;
      cursor: pointer;
      margin-top: 20px;
      width: 100%;
      font-weight: bold;
      transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.5s ease;
    }

    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(61, 206, 150, 0.4);
    }

    button:active {
      transform: translateY(0);
    }

    button:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    #result {
      margin-top: 30px;
      padding: 20px;
      background: var(--result-bg);
      border-radius: 10px;
      border-left: 4px solid var(--bg-gradient-start);
      white-space: pre-wrap;
      word-wrap: break-word;
      line-height: 1.6;
      font-size: 16px;
      color: var(--text-primary);
      min-height: 50px;
      transition: background 0.5s ease, color 0.5s ease, border-color 0.5s ease;
      opacity: 0;
      animation: fadeIn 0.5s ease forwards;
    }

    #result:not(:empty) {
      animation: slideInResult 0.6s ease forwards;
    }

    @keyframes slideInResult {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .analyzing {
      color: var(--bg-gradient-start);
      font-style: italic;
    }

    .error {
      color: #e74c3c;
      border-left-color: #e74c3c;
    }

    /* Auth Modal */
    .modal {
      display: none;
      position: fixed;
      z-index: 1000;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      justify-content: center;
      align-items: center;
    }

    .modal.active {
      display: flex;
    }

    .modal-content {
      background: var(--modal-bg);
      padding: 30px;
      border-radius: 15px;
      max-width: 400px;
      width: 90%;
      box-shadow: 0 10px 40px var(--shadow-color);
      transition: background 0.5s ease, box-shadow 0.5s ease;
      animation: modalSlideIn 0.3s ease forwards;
    }

    @keyframes modalSlideIn {
      from {
        opacity: 0;
        transform: scale(0.9) translateY(-20px);
      }
      to {
        opacity: 1;
        transform: scale(1) translateY(0);
      }
    }

    .modal-header {
      margin-bottom: 20px;
      text-align: center;
    }

    .modal-header h3 {
      color: var(--text-primary);
      margin-bottom: 10px;
      transition: color 0.5s ease;
    }

    .modal-body input {
      margin-bottom: 15px;
    }

    .modal-footer {
      display: flex;
      gap: 10px;
      margin-top: 20px;
    }

    .modal-footer button {
      flex: 1;
      margin: 0;
      padding: 12px;
      font-size: 16px;
    }

    .close-btn {
      background: #6c757d;
    }

    .auth-error {
      color: #e74c3c;
      font-size: 14px;
      margin-top: 10px;
      text-align: center;
    }

    /* Theme Toggle Button */
    .theme-toggle {
      background: var(--input-bg);
      border: 2px solid var(--input-border);
      color: var(--text-primary);
      padding: 8px 15px;
      border-radius: 20px;
      cursor: pointer;
      font-size: 18px;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .theme-toggle:hover {
      transform: scale(1.05);
      box-shadow: 0 2px 8px var(--shadow-color);
    }

    /* Input sections animation */
    .input-section.animate-in {
      animation: slideInFromLeft 0.6s ease forwards;
    }

    /* Landing Page Styles */
    .landing-page {
      min-height: calc(100vh - 100px);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 60px 20px;
      opacity: 0;
      animation: fadeInBody 1s ease 0.3s forwards;
    }

    .hero-content {
      max-width: 1000px;
      width: 100%;
      text-align: center;
      opacity: 0;
      transform: translateY(30px);
      animation: slideUpFade 1s ease 0.5s forwards;
    }

    @keyframes slideUpFade {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .hero-title {
      font-size: 3.5em;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 20px;
      background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      line-height: 1.2;
      transition: color 0.5s ease;
    }

    .hero-subtitle {
      font-size: 1.3em;
      color: var(--text-secondary);
      margin-bottom: 50px;
      line-height: 1.6;
      max-width: 700px;
      margin-left: auto;
      margin-right: auto;
      transition: color 0.5s ease;
    }

    .hero-features {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 30px;
      margin-bottom: 50px;
      opacity: 0;
      animation: fadeInUp 1s ease 0.8s forwards;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .feature-card {
      background: var(--container-bg);
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 5px 20px var(--shadow-color);
      transition: all 0.3s ease, background 0.5s ease, box-shadow 0.5s ease;
      border: 1px solid var(--input-border);
    }

    .feature-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 30px var(--shadow-color);
    }

    .feature-icon {
      font-size: 3em;
      margin-bottom: 15px;
    }

    .feature-card h3 {
      font-size: 1.3em;
      color: var(--text-primary);
      margin-bottom: 10px;
      transition: color 0.5s ease;
    }

    .feature-card p {
      color: var(--text-secondary);
      font-size: 1em;
      line-height: 1.5;
      transition: color 0.5s ease;
    }

    .hero-cta {
      display: flex;
      gap: 20px;
      justify-content: center;
      flex-wrap: wrap;
      opacity: 0;
      animation: fadeInUp 1s ease 1s forwards;
    }

    .cta-primary, .cta-secondary {
      padding: 18px 40px;
      font-size: 1.1em;
      font-weight: 600;
      border-radius: 12px;
      border: none;
      cursor: pointer;
      transition: all 0.3s ease;
      min-width: 200px;
    }

    .cta-primary {
      background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
      color: white;
      box-shadow: 0 4px 15px rgba(61, 206, 150, 0.3);
    }

    .cta-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(61, 206, 150, 0.4);
    }

    .cta-secondary {
      background: var(--input-bg);
      color: var(--text-primary);
      border: 2px solid var(--input-border);
      transition: all 0.3s ease, background 0.5s ease, color 0.5s ease, border-color 0.5s ease;
    }

    .cta-secondary:hover {
      transform: translateY(-2px);
      background: var(--result-bg);
      border-color: var(--bg-gradient-start);
    }

    .logo {
      font-size: 1.8em;
      font-weight: 700;
      background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    /* Smooth scroll behavior */
    html {
      scroll-behavior: smooth;
    }

    /* Hide analyzer section initially */
    #analyzerSection {
      opacity: 0;
      transform: translateY(50px);
      transition: opacity 0.8s ease, transform 0.8s ease;
    }

    #analyzerSection.visible {
      opacity: 1;
      transform: translateY(0);
    }
  </style>
</head>
<body>
  <!-- Navigation Bar -->
  <div class="navbar">
    <h1 class="logo">🔍 FakeNews Detector</h1>
    <div class="user-info">
      <button class="theme-toggle" onclick="toggleTheme()" id="themeToggle" title="Toggle theme">
        <span id="themeIcon">🌙</span>
        <span id="themeText">Dark</span>
      </button>
      <span id="userEmail" style="display: none;"></span>
      <button id="historyBtn" class="nav-btn nav-btn-primary" onclick="window.location.href='history.html'" style="display: none;">📜 History</button>
      <button id="loginBtn" class="nav-btn nav-btn-primary" onclick="showLoginModal()">Login</button>
      <button id="signupBtn" class="nav-btn nav-btn-secondary" onclick="showSignupModal()">Sign Up</button>
      <button id="logoutBtn" class="nav-btn nav-btn-secondary" onclick="logout()" style="display: none;">Logout</button>
    </div>
  </div>

  <!-- Landing Page Hero Section -->
  <div id="landingPage" class="landing-page">
    <div class="hero-content">
      <h1 class="hero-title">Welcome to FakeNews Detector</h1>
      <p class="hero-subtitle">Your intelligent AI-powered tool for detecting fake news and misinformation. Analyze articles instantly with cutting-edge technology.</p>
      
      <div class="hero-features">
        <div class="feature-card">
          <div class="feature-icon">📝</div>
          <h3>Text Analysis</h3>
          <p>Paste news text directly for instant analysis</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">🔗</div>
          <h3>URL Analysis</h3>
          <p>Enter article URLs for automated content extraction</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">🤖</div>
          <h3>AI Powered</h3>
          <p>Powered by Google Gemini AI for accurate detection</p>
        </div>
      </div>

      <div class="hero-cta">
        <button class="cta-primary" onclick="scrollToAnalyzer()">Get Started</button>
        <button class="cta-secondary" onclick="showSignupModal()">Create Account</button>
      </div>
    </div>
  </div>

  <!-- Main Container -->
  <div class="container" id="analyzerSection">
    <h2>Analyze News</h2>

    <!-- Input Tabs -->
    <div class="input-tabs">
      <button class="tab-btn active" onclick="switchTab('text')" id="textTabBtn">📝 Text</button>
      <button class="tab-btn" onclick="switchTab('url')" id="urlTabBtn">🔗 URL</button>
    </div>

    <!-- Text Input Section -->
    <div id="textSection" class="input-section">
      <textarea id="newsInput" rows="10" 
        placeholder="Paste news text here to analyze..."></textarea>
    </div>

    <!-- URL Input Section -->
    <div id="urlSection" class="input-section">
      <input type="url" id="urlInput" 
        placeholder="Enter news article URL (e.g., https://example.com/article)"
        onkeypress="if(event.key==='Enter') checkNews()">
      <small style="color: var(--text-secondary); display: block; margin-top: 5px; transition: color 0.5s ease;">
        We'll extract the text content from the URL for analysis
      </small>
    </div>

    <button onclick="checkNews()" id="checkBtn">🔍 Check News</button>

    <div id="result"></div>
  </div>

  <!-- Login Modal -->
  <div id="loginModal" class="modal">
    <div class="modal-content">
      <div class="modal-header">
        <h3>Login</h3>
      </div>
      <div class="modal-body">
        <input type="email" id="loginEmail" placeholder="Email" required onkeypress="if(event.key==='Enter') login()">
        <input type="password" id="loginPassword" placeholder="Password" required onkeypress="if(event.key==='Enter') login()">
        <div id="loginError" class="auth-error"></div>
      </div>
      <div class="modal-footer">
        <button onclick="login()">Login</button>
        <button class="close-btn" onclick="closeModal('loginModal')">Cancel</button>
      </div>
    </div>
  </div>

  <!-- Signup Modal -->
  <div id="signupModal" class="modal">
    <div class="modal-content">
      <div class="modal-header">
        <h3>Sign Up</h3>
      </div>
      <div class="modal-body">
        <input type="email" id="signupEmail" placeholder="Email" required onkeypress="if(event.key==='Enter') signup()">
        <input type="password" id="signupPassword" placeholder="Password (min 6 characters)" required onkeypress="if(event.key==='Enter') signup()">
        <div id="signupError" class="auth-error"></div>
      </div>
      <div class="modal-footer">
        <button onclick="signup()">Sign Up</button>
        <button class="close-btn" onclick="closeModal('signupModal')">Cancel</button>
      </div>
    </div>
  </div>

  <!-- Firebase SDK -->
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-auth-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore-compat.js"></script>
  <script src="firebase-config.js"></script>
  <script src="script.js"></script>
</body>
</html>
